import { Typography, Box } from '@mui/material';

export default function DashboardPage() {
  return (
    <Box>
      <Typography variant="h5" sx={{ mb: 2 }}>대시보드</Typography>
      <Typography color="text.secondary">Phase 1 구현 후 활성화됩니다.</Typography>
    </Box>
  );
}
